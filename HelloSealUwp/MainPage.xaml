<!-- SECURITY WARNING (PoC): Secret is stored in plaintext in app local storage. This is INSECURE and for demo only.
This PoC uses ZERO ENCRYPTION. Only Windows Hello signature verification gates access. -->
<Page
    x:Class="HelloSealUwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloSealUwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="12">
            <TextBlock Text="HelloSeal UWP" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="Proof of concept: Secret gated by Windows Hello signature verification." TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,24,0,0" Spacing="24">
            <StackPanel>
                <TextBlock Text="Enroll" FontSize="20" FontWeight="SemiBold" />
                <TextBox x:Name="EnrollSecretTextBox" Header="Secret (stored plaintext)" PlaceholderText="Hello world" />
                <TextBox x:Name="EnrollKeyNameTextBox" Header="Key Name" Text="HelloSealKey" />
                <Button Content="Enroll" Click="OnEnrollClicked" Margin="0,12,0,0" />
                <TextBlock x:Name="EnrollStatusTextBlock" Text="" TextWrapping="Wrap" />
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Unlock" FontSize="20" FontWeight="SemiBold" />
                <TextBox x:Name="UnlockKeyNameTextBox" Header="Key Name" Text="HelloSealKey" />
                <Button Content="Unlock" Click="OnUnlockClicked" Margin="0,12,0,0" />
                <TextBlock x:Name="UnlockStatusTextBlock" Text="" TextWrapping="Wrap" />
                <TextBlock x:Name="SecretOutputTextBlock" Text="" TextWrapping="Wrap" FontSize="18" FontWeight="SemiBold" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
